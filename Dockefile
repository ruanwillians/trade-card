FROM node:18-alpine

WORKDIR /app
COPY package*.json ./
RUN npm install
COPY . .
RUN npm run build
RUN npm install -g http-server

ENV PORT=8080

EXPOSE 8080

CMD ["http-server", "dist/spa", "-p", "8080"]